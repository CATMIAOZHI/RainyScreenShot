# taste.md — 协作偏好（RainyScreenShot）

本文档记录水晴喵与雨晴喵在本项目开发中的协作偏好。**每个任务开始前必须先读取本文件**，任务全程遵守其中约定。（继承自 RainyToken 项目，经水晴喵确认沿用）

## 审计流程（强制）

1. **修改后独立审计**：完成代码修改后，必须派发**独立 subagent** 进行审计，确认：
   - 无阻断问题（编译失败、逻辑错误、数据流断裂、回归风险）
   - 无影响用户体验的问题（UI 展示错误、交互异常、数值显示错误、异常路径处理）
2. **修复后复审**：若审计发现问题并完成修复，必须使用**同一个 subagent**（复用 task_id 续接）再次审计，确认问题已解决，避免引入新版问题。
3. **审计通过后才可收尾**：提交推送 / 发版 / 宣告完成之前，必须已有审计通过结论。

## 其他协作偏好

- **不装机不真机验证**：修改完成后以构建验证 + 独立审计闭环为准，不安装 APK 到真机、不做真机验证（除非水晴喵明确要求）。
- **实测优先**：解释数据、下结论前先用真实凭据/日志/接口实测验证，不臆断（shell 命令行为以 docs/TECH_NOTES.md 实测记录为准，与文档冲突时以实测为准并回写 TECH_NOTES.md）。
- **未经用户允许禁止提交推送**（见 AGENTS.md）。
- **路径规范**：派发 subagent 时，prompt 中所有工作区文件路径必须使用完整绝对路径（以工作区根目录为前缀），子代理没有工作区附着上下文。
- **改前理解**：修改任何文件前先理解相关代码及上下文，遵循项目既有惯例，不搞发明创造。
- **版本发布惯例**：版本 bump 独立 commit（`release: bump to vX.Y.Z`），轻量 tag 触发自动 Release。
- **参考惯例**：RainyToken 是本项目技术栈参考（Compose + MVVM + Hilt + Version Catalog + 雨晴粉主题），新代码风格对齐 RainyToken。

## 项目特定提醒

- **隐身性是核心卖点**：任何改动不得引入可感知信号（系统弹窗、MediaProjection、状态栏指示、目标 App 可见的变化）。若某功能必须引入可感知信号（如音频内录需 MediaProjection），须先征得水晴喵同意再实现。
- **凭据安全**：设备标识、display-id 等本机参数只用于实测，不硬编码进业务代码（通过运行时探测或设置项注入）。